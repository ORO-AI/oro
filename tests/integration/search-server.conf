server {
    listen 5632;

    location = /health {
        default_type text/plain;
        return 200 "healthy\n";
    }

    location = /find_product {
        default_type application/json;
        return 200 "[]\n";
    }
}
